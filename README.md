# SimpleLaMaEraser

SimpleLaMaEraser は、画像とマスク画像をブラウザからアップロードして、AI による消去処理を行うローカル専用の簡易 Web アプリです。  
不要物の除去、写り込みの削除、背景の一部修正などを、できるだけシンプルな手順で試せるようにしています。

このツールはインターネット公開を前提としておらず、個人の PC または家庭内 LAN での利用を想定しています。

## 特徴

- ブラウザから画像をドラッグ＆ドロップして使える
- 元画像とマスク画像を指定するだけで AI 消去を実行できる
- ローカル環境で完結する
- セットアップ用の `setup.bat` と起動用の `run.bat` を用意
- 1 回に 1 件だけ処理する安全な動作に調整済み

## 動作環境

- Windows
- Python 3.10
- NVIDIA GPU
- CUDA が利用可能な PyTorch 動作環境
- Git
- インターネット接続（初回セットアップ時のみ。依存パッケージとモデルの取得に使用）

## 事前準備

以下をインストールしてください。

- Python 3.10
- Git

`setup.bat` は `py -3.10` を使って Python 3.10 の仮想環境を作成します。  
そのため、Windows の Python Launcher が使える状態で Python 3.10 が入っている必要があります。

## インストール

1. このリポジトリを任意の場所に配置します。
2. `setup.bat` を実行します。
3. セットアップ完了後、`appfiles\lama` 以下に実行環境が作成されます。

`setup.bat` では以下を自動で行います。

- `appfiles` ディレクトリの作成
- LaMa 関連リポジトリの取得
- 必要ファイルの配置
- Python 3.10 仮想環境の作成
- 依存パッケージのインストール
- モデルデータのダウンロードと展開

## 起動方法

1. `run.bat` を実行します。
2. ブラウザで以下を開きます。

```text
http://localhost:7859
```

家庭内 LAN から利用する場合は、この PC の IP アドレスを指定してアクセスしてください。

## 使い方

1. 元画像をアップロードします
2. マスク画像をアップロードします
3. `Run` ボタンを押します
4. 処理結果が表示されたら、必要に応じてダウンロードします

### マスク画像について

マスク画像は白黒の画像を使用してください。

- 白: 消去したい領域
- 黒: 残したい領域

境界の扱いを安定させるため、マスクは完全な二値画像にしておくことをおすすめします。

## 対応形式

入力として一般的な画像形式を受け付けます。  
ただし、安定した運用のためには PNG の使用をおすすめします。

出力は PNG です。

## 仕様と注意点

- このツールはローカル専用です
- 公開サーバーとしての利用は想定していません
- 同時に複数の処理は行いません
- 処理中に新しいリクエストが来た場合はエラーになります
- アップロードサイズ上限は 64MB です
- 出力ファイルは一時的な利用を前提としています
- 処理内容や GPU 環境によって実行時間は変わります

## ディレクトリ構成

```text
SimpleLaMaEraser/
├─ app.py
├─ index.html
├─ main.js
├─ style.css
├─ require.txt
├─ setup.bat
├─ run.bat
└─ appfiles/
   └─ lama/
```

セットアップ完了後の実行本体は主に `appfiles\lama` 配下に作成されます。

## トラブルシューティング

### `py -3.10` が見つからない

Python 3.10 が正しくインストールされていないか、Python Launcher が利用できない可能性があります。  
Python 3.10 を再インストールし、Windows 用の標準的なセットアップを利用してください。

### GPU が見つからず起動しない

このツールは CUDA が利用可能な NVIDIA GPU 環境を前提としています。  
GPU ドライバや CUDA 対応状況、PyTorch のインストール状態を確認してください。

### `setup.bat` 実行中に失敗する

以下を確認してください。

- Python 3.10 が入っているか
- `py -3.10` が実行できるか
- Git が使えるか
- インターネット接続があるか

### アクセスしても画面が開かない

`run.bat` 実行後にコンソールへ表示されるメッセージを確認してください。  
ブラウザで `http://localhost:7859` を開いているか、ポートが他のアプリと競合していないかも確認してください。

## ライセンスと補足

このリポジトリは、ローカルで簡単に AI 消去を試すためのラッパーアプリです。  
内部では関連モデルおよび依存ライブラリを利用しています。各依存先のライセンスや利用条件もあわせて確認してください。
